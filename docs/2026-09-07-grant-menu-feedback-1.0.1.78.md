# 1.0.1.78 菜单回显修复候选

只读检查 2026-09-07 04:19 的最新存档：GrantSetting 有 68 条记录，GrantEvent 有 75 条；Battleaxes、MusicalInstrument 等已记录为 0。因此点击并非完全失效，不能以没有勾选推断没有执行。

75 个新增菜单镜像被动使用 IsHidden，与现有可工作的配置镜像及 verify.ps1 的 Stats.Passives 可枚举约束相冲突。本版移除这 75 项的隐藏属性，提供已有本地化名称、说明和图标。基础隐藏被动保持不变。

起源身份在已有 StatusApplied/StatusRemoved 的身份标签处理规则中调用镜像同步；未恢复 .72 新增的两条监听规则。编译仍为 6 goals、1253 nodes、567 constants。

新增回归检查分别先失败于隐藏镜像、缺失状态同步，再通过。完整构建和反向校验通过。

已安装并导出桌面博德之门3mod目录，版本 1.0.1.78，Version64 36028799166447694。
SHA256: 55124BB64DD1C48F1782F1C135C639B3FF76B900D8CCF544C53EDDB849C5AC71

安装时游戏未运行；除混沌版本号之外，加载配置规范化哈希不变。未改动存档、其他 MOD，未推送 GitHub。

待游戏内验证：读档正常；菜单熟练项勾选与实际切换一致；起源技能栏切换后菜单一致，菜单切换后技能栏一致。保存中已有关闭选项应保持关闭，不强制重置为开启。
