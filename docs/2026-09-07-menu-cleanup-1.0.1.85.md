# 1.0.1.85 菜单修正

- 键鼠菜单五组全选/反选使用与底部按钮相同的 BigBrownButtonStyle。
- 手柄菜单保留其原生控件，不引用键鼠专属样式资源。
- 移除误列入种族菜单的内部 VO_POSTPROCESS 标签，移除批量操作成员。
- 重新同步时撤销旧存档中的该项菜单映射，避免旧全选成员继续生效；不清除角色原生标签。
- 新增回归检查，先确认旧代码缺少样式且错误包含内部标签，再验证修正。
- 两个种族总开关仍未包含在本版；游戏内外观和操作待用户验收。
