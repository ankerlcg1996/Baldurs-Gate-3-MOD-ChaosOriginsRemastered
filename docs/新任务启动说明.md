# 后续任务启动说明

工作目录：`C:\Users\ankerlcg\Desktop\chaos-BG3-mod-story`

原生 Story 版已经实现并能编译打包。继续工作前阅读 `README.md`、本目录的迁移说明和功能矩阵。`reference-se/` 只用于核对 SE `1.0.25` 行为；所有改动写入 `story-src/`。

构建命令：

```powershell
& .\story-src\build.ps1
```

当前最重要的工作不是继续堆功能，而是进行 BG3 实机验收：角色创建/守护者、1–12 级升级、伤害边界、剧情 Flag 和 SE 共存。发现问题时先记录真实事件、伤害数值、状态与存档阶段，再修改规则；不要添加猜测性 fallback。
