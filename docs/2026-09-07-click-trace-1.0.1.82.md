# 1.0.1.82 菜单点击诊断版（不是已修复版本）

用户确认起源身份、熟练项、瓦罗之眼点击后没有效果。
1.0.1.81 游戏 Story 日志为零编译错误；最新可读存档设置已初始化。
尚无足够证据区分 UI 事件未送达、角色/战斗条件拦截、执行后显示异常。

本版仅为这些事件添加 DB_COS_ConfigClickTrace(Character, Key, Stage)。
Received 表示事件到达（不受角色/战斗权限条件影响）；Applied 表示原有处理
分支已执行，不等于引擎最终状态或界面已更新。记录为累计出现过的阶段，
不是计数器，也不用于断言重复点击次数。配套 DebugText 提供临时提示。
不修改权限、概率、能力效果、开关值或 UI 布局。

verify-click-trace.ps1 先失败后通过；完整构建及反向校验通过。
新增写入数据库首次被编译器拒绝为未读取，随后添加收到记录时的提示读取规则，
再次编译通过。没有放宽编译检查。

测试：读档后分别点击上述三类中的一个选项，退出菜单观察诊断提示并保存。
下一步只读比较 Trace、真实设置以及角色状态，定位后移除诊断提示。
游戏内故障仍未解决或验收，不应将构建成功当作修复。

已在游戏退出后替换安装包，导出桌面博德之门3mod目录；仅更新本模块版本号。
