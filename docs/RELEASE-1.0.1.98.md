# 混沌起源 1.0.1.98 发布候选

日期：2026-09-14
功能基线提交：`54c1f8c`
状态：已完成静态验证、构建、导出和本机安装；尚未完成实机验收。

## 本版内容

- 在原生设置菜单加入只读运行诊断面板，同时覆盖键鼠与手柄页面。
- 当前状态区分非混沌起源、配置不完整、核心镜像不一致和就绪。
- 最近问题状态共 28 种（含“无记录”），其余状态对应明确的缺失或不一致原因。
- 显示混沌之力与剩余掌控混沌点数；诊断只读取现有状态，不修改设置、资源或玩法效果。
- 打开设置菜单时先诊断、执行既有配置同步、再诊断，保留原有同步行为。

## 版本与包

- 显示版本：`1.0.1.98`
- Version64：`36028799166447714`
- 正式文件：38 个，反向解包清单与 `package-files.json` 完全一致，无额外文件。
- 源 PAK：`dist/ChaosOriginsStory.pak`
- 桌面候选：`C:\Users\ankerlcg\Desktop\博德之门3mod\ChaosOriginsStory-1.0.1.98.pak`
- 大小：`571208` 字节
- SHA-256：`5fbfb668fcee7638b779bf8801c4e9f058832c9697a0964f84767db2353a909e`
- `dist/build-manifest.json` 记录的 PAK 哈希与源文件、桌面候选一致。

## 本机安装

- 已安装到 `Baldur's Gate 3\Mods\ChaosOriginsStory.pak`。
- Mods 目录仅保留 1 个 `ChaosOriginsStory*.pak`。
- `modsettings.lsx` 仅保留 1 条混沌起源加载项，`Version64` 为 `36028799166447714`。
- 安装 PAK 的 SHA-256 与源 PAK、桌面候选和构建 manifest 一致。

## 静态验证

- 构建前 `story-src/verify.ps1`：PASS，基线为 `lastBuild=97`、38 个正式文件。
- 正式构建：仅调用一次；Story、资源、IR 证明、PAK 创建、反向解包和逐文件 SHA-256 核对全部通过。
- Story 编译：PASS，6 个 Goal、1760 个节点、864 个有效常量。
- 运行诊断专用验证：PASS，32 个诊断状态、71 个本地化句柄、41 个 Story 块；键鼠/手柄只读布局与变异检查通过。
- 构建后 `story-src/verify.ps1`：PASS。
- 后续构建会从 `version.json` 计算下一版本，并将其作为严格预检参数传入四语版本文本验证；预检失败不会写入版本号。

## 游戏内验收

以下项目全部为 **PENDING**，本记录不宣称实机通过：

- 键鼠与手柄页面的显示、滚动和只读行为。
- 打开菜单及配置变化后的诊断状态刷新。
- 非混沌起源、配置缺失、核心镜像不一致和 Ready 四类状态。
- 最近问题、混沌之力和掌控混沌剩余点数的显示。
- `1.0.1.97` 旧存档与新建存档的加载、角色创建、加载顺序和正常游玩。

只有本候选完成上述实机验收后，才开始下一阶段功能。
