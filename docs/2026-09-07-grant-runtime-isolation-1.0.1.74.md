# .74 自动授予入口隔离测试

## 用户验证与范围

- .71 正常；.72 无法进入；.73（.72 资源 + .71 整套 Story）安装后用户确认正常。
- .73 说明故障随新增 Story 撤回而消失，具体规则或资源交互尚未确定。
- .74 恢复 .72 Story，唯独去除两个 `PROC_COS_ConfigSyncGrants(_Character)` 调用：BaseAfterCreation 的单动作重载，以及 ConfigSyncCharacter 的第一条动作。
- 其余新增过程、数据库、事件规则全部保留。没有修改游戏菜单、Stats 或本地化。
- 常规构建仍要求自动入口存在；本测试使用 `build.ps1 -GrantRuntimeIsolation`，额外运行 `verify-grant-runtime-isolation.ps1`。

## 验证

- 校验先对 .72 失败：自动入口仍连接。
- .74 源码和编译规则检查通过：280 条规则（.72 为 281），6 goals、1258 nodes、571 valid constants。
- 38 条包文件核对通过：仅两个源码 Goal、编译 Story、meta 版本与 .72 不同。
- 重新生成的 LSF 二进制不稳定；隔离模式复制 .72 原始非 Story 文件，保持资源字节一致，而不是放宽比较。
- 版本 1.0.1.74，Version64 `36028799166447690`。
- SHA256 `96DB12FD3CD301F229294EA0CA74B79CAF70FB377F9E6F8034EDF420F902F619`。
- 已放入桌面 `博德之门3mod/ChaosOriginsStory-1.0.1.74-runtime-isolation.pak` 并安装为游戏 `Mods/ChaosOriginsStory.pak`。
- 已更新且仅更新混沌条目的版本号。其他加载配置归一化哈希前后相同：`A00F4D42BF60D82ED1BEF70E08BFDD50EF2CB1B478C5E6B2A06FC986326ED867`。
- 未关闭游戏进程：安装检查时游戏已退出。未修改存档、未创建桌面备份、未提交或推送 GitHub。

## 下一步判读

请只测试能否进入角色创建，不要保存，不要操作新开关。新开关和分项熟练项授予仍不完整。

- .74 仍失败：自动授予流程不是必要触发条件，优先拆查新增规则的加载/合并结构与残留事件路径。
- .74 正常：重点拆查自动同步路径，不能仅凭这一结果宣布具体函数已定位。

当前开发源码保留 .74 的断开状态；下一次正式修复必须恢复两个入口。正常构建会拦截未恢复的状态，不能把隔离包称为功能完整版。
下一版本末位从 75 继续。当前游戏验收仍待用户反馈。
