# 1.0.1.93：负重菜单修复候选

- 负重复选框改用独立状态镜像，不再直接读取隐藏的负重功能被动；镜像本身不提供加成。
- 打开菜单时初始化缺失的负重设置并同步效果、勾选状态和点击事件；已有关闭值不被默认值覆盖。
- 删除键鼠和手柄菜单中的“已开启额外设置”汇总列表，保留混沌总览、最近受击记录及实际设置控件。
- 不调整混沌概率、种族能力、身份或熟练项机制。

## 验证边界

源代码回归检查、Story 编译和 PAK 反向检查已通过。版本为 1.0.1.93，Version64 为 36028799166447709。
这些检查不等同于游戏内验证，也不代表已安装。

待游戏内验证：旧存档打开菜单后负重勾选正确；取消后负重上限下降，开启后恢复；重复开关不叠加；汇总列表不再出现。
