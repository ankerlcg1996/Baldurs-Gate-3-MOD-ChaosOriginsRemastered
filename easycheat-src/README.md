# EasyCheat 日常增益持久化补丁

该补丁只改变“日常增益”自绘菜单的保存行为：

- 每次改变总开关、特效、音效或任一增益勾选后，立即写入 `EasyCheat/DailyBuffSettings.json`。
- 启动游戏、载入关卡或脚本上下文重置后，自动恢复最后一次保存的勾选状态。
- 不改变 EasyCheat 其他作弊功能、日常增益内容或施加规则。

构建依赖本地保存的原始 PAK，并校验其 SHA256；每次成功构建仅将末位版本号加一。
